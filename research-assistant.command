#!/bin/bash
# Double-click this file (macOS) to launch the Research Assistant.
# First run: right-click → Open (to bypass Gatekeeper on an unsigned script).
cd "$(dirname "$0")"
exec bash scripts/research-assistant.sh
